# 배포 가이드

## GitHub repo 권장 구조

이 `pdf_binding_sample_tool` 폴더의 내용만 별도 GitHub 저장소로 올리는 것을 권장합니다.

- 저장소 공개 범위: 사내 운영 도구라면 `Private`
- 기본 브랜치: `main`
- 배포 방식: GitHub Release에 ZIP 파일 첨부

## Streamlit Cloud 배포 설정

- Repository: `jongminpark51/pod_pdf_binding_tool`
- Branch: `main`
- Main file path: `app.py`
- Python version: `3.12`

Streamlit Cloud에서 Python 버전은 repo 파일로 강제되지 않습니다. 로그에 `Python 3.14.x`처럼 다른 버전이 보이면 앱을 삭제한 뒤 같은 subdomain으로 다시 배포하면서 Advanced settings에서 Python `3.12`를 선택합니다.

일시적인 `keepalive ping timeout` 또는 `ConnectionClosedError`가 발생했지만 코드 변경 직후가 아니라면 먼저 앱을 reboot합니다. 같은 문제가 반복되면 Python 버전과 GitHub Actions 결과를 확인합니다.

## Release ZIP 만들기

개발자 PC에서 아래 파일을 실행합니다.

```powershell
make_release_zip.bat
```

생성 위치:

```text
dist/pdf_binding_sample_tool_v1.1.1.zip
```

ZIP 파일에는 `.venv`, 캐시, 로그, `dist` 폴더가 포함되지 않습니다.

## 운영자에게 전달

1. GitHub Release에서 ZIP 파일을 내려받습니다.
2. 원하는 위치에 압축을 풉니다.
3. 최초 1회 `install_windows.bat`을 실행합니다.
4. 이후 `run_windows.bat`으로 실행합니다.

## GitHub Actions

`.github/workflows/smoke-test.yml`이 포함되어 있어 push 또는 pull request마다 Windows와 Linux 환경에서 PDF 생성/복원 스모크 테스트를 실행합니다.
