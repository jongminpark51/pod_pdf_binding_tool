# 배포 가이드

## GitHub repo 권장 구조

이 `pdf_binding_sample_tool` 폴더의 내용만 별도 GitHub 저장소로 올리는 것을 권장합니다.

- 저장소 공개 범위: 사내 운영 도구라면 `Private`
- 기본 브랜치: `main`
- 배포 방식: GitHub Release에 ZIP 파일 첨부

## Release ZIP 만들기

개발자 PC에서 아래 파일을 실행합니다.

```powershell
make_release_zip.bat
```

생성 위치:

```text
dist/pdf_binding_sample_tool_v1.1.1.zip
```

ZIP 파일에는 `.venv`, 캐시, 로그, `dist` 폴더가 포함되지 않습니다.

## 운영자에게 전달

1. GitHub Release에서 ZIP 파일을 내려받습니다.
2. 원하는 위치에 압축을 풉니다.
3. 최초 1회 `install_windows.bat`을 실행합니다.
4. 이후 `run_windows.bat`으로 실행합니다.

## GitHub Actions

`.github/workflows/smoke-test.yml`이 포함되어 있어 push 또는 pull request마다 Windows 환경에서 PDF 생성 스모크 테스트를 실행합니다.
