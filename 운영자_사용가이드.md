# 운영자 사용가이드

## 처음 한 번만 설치

`install_windows.bat` 파일을 더블클릭합니다.

- Python이 없으면 자동 설치를 시도합니다.
- 필요한 프로그램은 이 폴더 안의 `.venv` 폴더에 설치됩니다.
- 설치가 끝나면 창에 `Installation complete`가 표시됩니다.

## 매번 실행

`run_windows.bat` 파일을 더블클릭합니다.

- 브라우저에서 PDF 제본 샘플 생성 화면이 열립니다.
- 처음 접속하면 공유된 운영자 암호를 입력해 로그인합니다.
- 파일을 업로드하고 병합 순서와 워터마크 밀도를 선택한 뒤 PDF를 생성합니다.
- 원본 PDF가 암호화되어 있고 내부 운영자 암호로 열리는 파일이면 자동으로 처리됩니다.
- 워터마크를 제거해야 할 때는 `워터마크 제거` 탭에서 생성된 PDF를 업로드합니다.
- 워터마크 제거는 여러 PDF를 한 번에 업로드할 수 있고, 다운로드 파일명은 기존 파일명 뒤에 다운로드 날짜가 붙습니다.

## 문제가 생겼을 때

- 설치가 실패하면 인터넷 연결을 확인한 뒤 `install_windows.bat`을 다시 실행합니다.
- 실행이 실패하면 `install_windows.bat`을 한 번 더 실행한 뒤 `run_windows.bat`을 실행합니다.
- 기존 설치를 초기화하려면 이 폴더 안의 `.venv` 폴더를 삭제한 뒤 다시 설치합니다.
